/* This file is just for situations where the langauge stated in the LANG
 * variable passed to make happens to contain an underscore, instead of
 * complex logic to filter it out we just include the intended file via a file
 * with a basename that matches what LANG contains */
#include "enGB.c"
